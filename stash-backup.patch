diff --git a/.env.example b/.env.example
index 38e378e..1056f3b 100644
--- a/.env.example
+++ b/.env.example
@@ -1 +1,8 @@
-DATABASE_URL=postgres://username:password@host:port/dbname?sslmode=require
+# Google Cloud Platform設定
+PROJECT_ID="your-project-id"
+SERVICE_NAME="your-service-name"
+REGION="your-region"
+REPO_NAME="your-repo-name"
+
+# データベース設定
+DATABASE_URL="your-database-url"
diff --git a/deploy.sh b/deploy.sh
index 40c1acf..2f60d2e 100755
--- a/deploy.sh
+++ b/deploy.sh
@@ -5,21 +5,15 @@ if [ -f .env ]; then
     export $(cat .env | grep -v '#' | xargs)
 fi
 
-# 環境変数が設定されているか確認
-if [ -z "$DATABASE_URL" ]; then
-    echo "Error: DATABASE_URL is not set"
-    echo "Please create .env file from .env.example"
-    exit 1
-fi
-
-# プロジェクトIDを既存のものに設定
-PROJECT_ID="striking-decker-449414-a6"
-# サービス名
-SERVICE_NAME="go-service"
-# リージョン
-REGION="asia-northeast1"
-# Artifact Registryのリポジトリ名
-REPO_NAME="go-apps"
+# 必要な環境変数が設定されているか確認
+required_vars=("DATABASE_URL" "PROJECT_ID" "SERVICE_NAME" "REGION" "REPO_NAME")
+for var in "${required_vars[@]}"; do
+    if [ -z "${!var}" ]; then
+        echo "Error: $var is not set"
+        echo "Please create .env file from .env.example"
+        exit 1
+    fi
+done
 
 # リポジトリが存在しない場合のみ作成
 if ! gcloud artifacts repositories describe $REPO_NAME --location=$REGION &>/dev/null; then
